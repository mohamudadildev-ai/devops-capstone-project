apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipelinerun-pvc
spec:
  storageClassName: skills-network-learner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
